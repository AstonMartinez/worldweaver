import { useState } from "react";
import EventQuestionnaire from "../../../utils/Questionnaires/Event/Combined";

const ThemeQuestions = ({ setThemes }) => {
  const [inputs, setInputs] = useState({
    one: "",
    two: "",
    three: "",
  });

  const handleInputChange = async (e) => {
    setInputs((prev) => ({ ...prev, [e.target.name]: e.target.value }));
  };
  return (
    <>
      <div>
        <h3>Themes</h3>
      </div>
      <div>
        <section>
          <div>
            <label htmlFor="one">{EventQuestionnaire.Cause[0]}</label>
          </div>
          <div>
            <textarea
              name="one"
              value={inputs.one}
              onChange={(e) => handleInputChange(e)}
            />
          </div>
        </section>
        <section>
          <div>
            <label htmlFor="two">{EventQuestionnaire.Cause[1]}</label>
          </div>
          <div>
            <textarea
              name="two"
              value={inputs.two}
              onChange={(e) => handleInputChange(e)}
            />
          </div>
        </section>
        <section>
          <div>
            <label htmlFor="three">{EventQuestionnaire.Cause[2]}</label>
          </div>
          <div>
            <textarea
              name="three"
              value={inputs.three}
              onChange={(e) => handleInputChange(e)}
            />
          </div>
        </section>
      </div>
      <div>
        <button onClick={() => setThemes(inputs)}>Save & Continue</button>
      </div>
    </>
  );
};

export default ThemeQuestions;
