import { useState } from "react";
import FactionQuestionnaire from "../../../utils/Questionnaires/Faction/Combined";

const TitleQuestions = ({ setTitles }) => {
  const [inputs, setInputs] = useState({
    one: "",
    two: "",
    three: "",
  });

  const handleInputChange = async (e) => {
    setInputs((prev) => ({ ...prev, [e.target.name]: e.target.value }));
  };
  return (
    <>
      <div>
        <h3>Name / Title</h3>
      </div>
      <div>
        <section>
          <div>
            <label htmlFor="one">
              {FactionQuestionnaire["Name / Title"][0]}
            </label>
          </div>
          <div>
            <textarea
              name="one"
              value={inputs.one}
              onChange={(e) => handleInputChange(e)}
            />
          </div>
        </section>
        <section>
          <div>
            <label htmlFor="two">
              {FactionQuestionnaire["Name / Title"][1]}
            </label>
          </div>
          <div>
            <textarea
              name="two"
              value={inputs.two}
              onChange={(e) => handleInputChange(e)}
            />
          </div>
        </section>
        <section>
          <div>
            <label htmlFor="three">
              {FactionQuestionnaire["Name / Title"][2]}
            </label>
          </div>
          <div>
            <textarea
              name="three"
              value={inputs.three}
              onChange={(e) => handleInputChange(e)}
            />
          </div>
        </section>
      </div>
      <div>
        <button onClick={() => setTitles(inputs)}>Save & Continue</button>
      </div>
    </>
  );
};

export default TitleQuestions;
